# Rule 
For each user request, it should be delegated to the agent `@orchestrator`.
## Example Usage
- User: "implement a login feature"
- Agent: "Delegating to the agent `@orchestrator` to implement login feature."